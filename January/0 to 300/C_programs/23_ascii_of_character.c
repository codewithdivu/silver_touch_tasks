#include <stdio.h>

int main()
{
    char ch;
    int answer;
    printf("enter a character for which you want ascii value");
    scanf("%c", &ch);
    answer = ch;
    printf("%d", answer);
    return 0;
}